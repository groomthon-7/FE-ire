import styled from "styled-components";
import CardBg from "../../assets/Research/smCard";

const SmallCard = ({ text, img }) => {
  return <Card>{text}</Card>;
};

const Card = styled.div``;

export default SmallCard;
